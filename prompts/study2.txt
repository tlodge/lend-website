I need to run a study that will send text or emails or WhatsApp messages out to participants. The messages will include links to media - it may be text, and image, sound or video.  The link will have open graph tags so that it displays elegantly.  When a user clicks on the link they are then shown a full summary of the media, with a button to view or play it.  The links will have a userId appended to them so we can track who has clicked on it.  So we probably need something like /narrrative/[type]/[nid]/[uid]. Where type is video,audio,text or image. The narratives that are available will be in /public/narratives, so we can use a (currently empty) public/narratives/collections.json file to list the narratives that are available.   The images used in open graph tags are at: public/narratives/thumbnails.   For now I just want to see what a link will look like when I send to WhatsApp - i.e. how the image displays. Can we create a test stub so that I can share a link e.g [url]/narrative/video/1/1 and see a nicely formatted link?